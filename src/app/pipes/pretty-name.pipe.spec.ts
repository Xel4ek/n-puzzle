import { PrettyNamePipe } from './pretty-name.pipe';

describe('PrettyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
